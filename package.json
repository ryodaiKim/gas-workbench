{
  "name": "visit-management-apps-script",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p . && node scripts/postbuild.mjs",
    "watch": "tsc -w -p .",
    "push": "npm run build && npx clasp push",
    "deploy": "npm run build && npx clasp push && npx clasp deployments && echo 'Consider creating a version and deploying'"
  },
  "description": "This project manages reminder emails from a Google Spreadsheet and logs dispatches. It uses clasp to develop locally and push to Apps Script.",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@google/clasp": "^3.0.6-alpha",
    "@types/google-apps-script": "^2.0.1",
    "typescript": "^5.9.2"
  }
}

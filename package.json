{
  "name": "gas_workbench",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "projects/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "push": "npm run push --workspaces --if-present"
  },
  "devDependencies": {
    "@google/clasp": "^3.0.6-alpha",
    "@types/google-apps-script": "^2.0.1",
    "typescript": "^5.9.2"
  }
}